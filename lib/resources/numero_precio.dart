import 'package:intl/intl.dart';

NumberFormat uSFormat = NumberFormat.currency(
  locale: "en_US",
  symbol: "\$",
  decimalDigits: 0,
);

NumberFormat dinero = NumberFormat.currency(
  locale: "en_US",
  symbol: "\$",
  decimalDigits: 0,
);
